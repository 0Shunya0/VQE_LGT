# SUPERSEDED

These figures were generated **before** the `build_H_full` / `build_operators` sign fix and are out of date.
Use `pra_figures/` at the repository root instead. Kept only for the record; do not use in the manuscript.
